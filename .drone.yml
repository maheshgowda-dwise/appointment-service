---
kind: pipeline
type: docker
name: build-push-update-stack

steps:
  - name: "maven-build"
    image: "maven:3.9-eclipse-temurin-17"
    commands:
      - "echo '🚀 Starting Maven Build...'"
      - "mvn -B -ntp clean install -DskipTests"
      - "echo '✅ Maven build completed successfully.'"

  - name: "prepare-tag"
    image: "alpine:latest"
    depends_on:
      - "maven-build"
    commands:
      - "apk add --no-cache bash coreutils"
      - "SHORT_COMMIT=$(echo ${DRONE_COMMIT_SHA} | cut -c1-7)"
      - "TIMESTAMP=$(date +%Y%m%d%H%M)"
      - "IMAGE_TAG=v2qa-${TIMESTAMP}-${SHORT_COMMIT}"
      - "echo ${IMAGE_TAG} > image_tag.txt"
      - "echo '✅ Generated tag:' $(cat image_tag.txt)"

  - name: "docker-build-push"
    image: "plugins/docker"
    depends_on:
      - "prepare-tag"
    settings:
      repo: "maheshgowdamg25/lt-be-apnt-service"
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
      dockerfile: "Dockerfile"
      context: "."
      tags:
        - "latest"
        - "v2qa-${DRONE_BUILD_NUMBER}-${DRONE_COMMIT_SHA:0:7}"

  - name: "update-stack-repo"
    image: "alpine/git"
    depends_on:
      - "docker-build-push"
    environment:
      STACK_REPO_URL: "https://github.com/maheshgowda-dwise/Argocd.git"
      STACK_BRANCH: "main"
      STACK_FILE: "LT-QA/BE-Projects/BE-Canary-Helm-Unified/values.yaml"
      VALUES_SERVICE_NAME: "appointment"
      DOCKER_REPO: "maheshgowdamg25/lt-be-apnt-service"
      GIT_USER:
        from_secret: git_user
      GIT_TOKEN:
        from_secret: git_token
    commands:
      - "apk add --no-cache bash git sed coreutils"
      - "IMAGE_TAG=$(cat image_tag.txt)"
      - "NEW_IMAGE=${DOCKER_REPO}:${IMAGE_TAG}"
      - "echo '🔁 Cloning stack repo...'"
      - "git clone --branch ${STACK_BRANCH} https://${GIT_USER}:${GIT_TOKEN}@${STACK_REPO_URL#https://} stack-repo"
      - "cd stack-repo"
      - "echo '🖋️ Updating ${STACK_FILE} with ${NEW_IMAGE}'"
      - "sed -i \"s|image:.*|image: ${NEW_IMAGE}|g\" ${STACK_FILE}"
      - "git config user.email 'mahesh.g@lifetrenz.com'"
      - "git config user.name 'maheshgowda-dwise'"
      - "git add ${STACK_FILE}"
      - "git commit -m \"chore(${VALUES_SERVICE_NAME}): update image to ${IMAGE_TAG}\" || echo '⚠️ No changes to commit'"
      - "git push origin ${STACK_BRANCH}"
      - "echo '🚀 Stack repo updated successfully with ${IMAGE_TAG}'"

trigger:
  branch:
    - "main"
  event:
    - "push"
    - "custom"
